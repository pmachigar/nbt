replicaCount: 1

image:
  repository: nbt/catalog-service
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 9092

mongodb:
  uri: mongodb://admin:password@nbt-mongo:27017/catalog-db?authSource=admin
