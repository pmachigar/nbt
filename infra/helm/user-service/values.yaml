replicaCount: 1

image:
  repository: nbt/user-service
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 9090

mongodb:
  uri: mongodb://admin:password@nbt-mongo:27017/user-db?authSource=admin
